export const animations = {
  transitionFast: "transition-all duration-250 ease-in-out",
  imageHover: "group-hover:scale-105 transition-transform duration-500 ease-out",
  cardHover: "hover:-translate-y-[6px] hover:shadow-xl transition-all duration-300 ease-out",
} as const;

export default animations;
